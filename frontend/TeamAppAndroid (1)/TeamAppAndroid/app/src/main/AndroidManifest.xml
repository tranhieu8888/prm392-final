<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.teamapp">

  <!-- Network -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <!-- Android 13+ notifications (runtime permission).
       Thêm tools:targetApi để tránh cảnh báo trên API thấp hơn. -->
  <uses-permission
      android:name="android.permission.POST_NOTIFICATIONS"
      tools:targetApi="33" />

  <application
      android:name=".App"
      android:allowBackup="true"
      android:icon="@drawable/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@drawable/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.TeamApp"
      android:networkSecurityConfig="@xml/network_security_config">

    <!-- Launcher -->
    <activity
        android:name=".ui.auth.LoginActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- Auth -->
<!--    <activity android:name=".ui.auth.LoginActivity" android:exported="false" />-->
    <activity android:name=".ui.auth.RegisterActivity" android:exported="false" />
    <activity android:name=".ui.main.MainActivity" android:exported="false" />

    <!-- Projects -->
    <activity android:name=".ui.project.ProjectsActivity" android:exported="false" />
    <activity android:name=".ui.project.CreateProjectActivity" android:exported="false" />
    <activity android:name=".ui.project.MembersActivity" android:exported="false" />
    <activity android:name=".ui.project.DiscoverProjectsActivity" android:exported="false" />

    <!-- Tasks -->
    <activity android:name=".ui.task.KanbanActivity" android:exported="false" />
    <activity android:name=".ui.task.CreateTaskActivity" android:exported="false" />
    <activity android:name=".ui.task.TaskDetailActivity" android:exported="false" />


    <!-- Chat -->
    <activity android:name=".ui.chat.ConversationsActivity" android:exported="false" />
    <activity android:name=".ui.chat.ChatActivity" android:exported="false" />

    <!-- Notifications & Profile -->
    <activity android:name=".ui.notif.NotificationsActivity" android:exported="false" />
    <activity android:name=".ui.profile.ProfileActivity" android:exported="false" />
    <activity android:name=".ui.profile.ChangePasswordActivity" android:exported="false" />

    <!-- Calendar & Search -->
    <activity android:name=".ui.calendar.CalendarActivity" android:exported="false" />
    <activity android:name=".ui.search.GlobalSearchActivity" android:exported="false" />

    <!-- (TÙY CHỌN) Firebase Cloud Messaging service.
         Nếu CHƯA có google-services.json hoặc chưa tích hợp FCM, hãy xoá hoặc comment block này để tránh lỗi build. -->

    <service
        android:name=".fcm.MyFirebaseService"
        android:exported="false">
        <intent-filter>
            <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
    </service>

    <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_app_logo" />


  </application>

</manifest>
